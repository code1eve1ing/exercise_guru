export const showLoginModal = (showModal)=>({
    type:'SHOW_LOGIN_MODAL',
    payload: showModal
})

export const showScheduleModal = (showModal)=>({
    type:'SHOW_SCHEDULE_MODAL',
    payload: showModal
})